# Backend Configuration
PORT=5000

# MongoDB Atlas Connection
# IMPORTANT: Replace with your actual MongoDB URI
# Do NOT commit your real MongoDB URI to version control
# For Vercel: Add this as Environment Variable (NOT Secret) in Vercel Dashboard
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=AppName
DB_NAME=eduflow

# Frontend Origins (comma-separated)
# Allowed domains that can make requests to this API
# For Vercel: Use your Vercel domain (e.g., https://yourapp.vercel.app)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Environment
NODE_ENV=development

# Google Gemini API (for AI Chat)
# Get your API key from: https://makersuite.google.com/app/apikey
# IMPORTANT: This is a backend secret, never expose to frontend
# For Vercel: Add this as Environment Variable in Vercel Dashboard
GEMINI_API_KEY=your_gemini_api_key_here

# Frontend Configuration
VITE_API_URL=http://localhost:5000/api
VITE_ADMIN_PASSWORD=admin123

# ============================================================================
# 🚀 VERCEL DEPLOYMENT INSTRUCTIONS
# ============================================================================
# 1. Do NOT use Secrets (@secret_name) in vercel.json
# 2. Add these variables as Environment Variables in Vercel Dashboard:
#    - Go to Project Settings > Environment Variables
#    - Add each variable with its actual value
# 3. Environment Variables needed for Vercel:
#    - MONGODB_URI (your MongoDB connection string)
#    - DB_NAME (database name)
#    - GEMINI_API_KEY (Google Gemini API key)
#    - ALLOWED_ORIGINS (your Vercel domain)
#    - NODE_ENV (set to "production")
# ============================================================================
